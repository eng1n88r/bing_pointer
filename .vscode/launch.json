{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Run Searches",
            "program": "${workspaceRoot}/bin/bing-pointer.js",
            "cwd": "${workspaceRoot}"
        },
        {
            "type": "node",
            "request": "launch",
            "name": "Setup Login",
            "program": "${workspaceRoot}/bin/bing-pointer.js",
            "args": ["--setup"],
            "cwd": "${workspaceRoot}"
        }
    ]
}
